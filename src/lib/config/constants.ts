// Shared configuration constants
export const SERVER_PORT = 3001;
export const CLIENT_PORT = 5173;
export const DEV_SERVER_URL = `http://localhost:${SERVER_PORT}`;
export const CLIENT_URL = `http://localhost:${CLIENT_PORT}`;